---
session: general
date: 2026-03-06
status: partial
outcome: PARTIAL_MINUS
---

goal: Implemented 3-step prompt rewriting pipeline (question extraction → blind codebase research → rewrite) on branch feature/prompt-rewriting-research
now: PARE DOWN the branch — strip the research phase entirely, keep only a single OpenAI-based prompt rewrite step using best practices for coding agents

test: npm run dev -- --rewrite-prompt "your task description" /path/to/target/repo

done_this_session:
  - task: Added --rewrite-prompt CLI flag to ivan
    files: [src/index.ts]
  - task: Built 3-step pipeline with OpenAI question extraction + Claude Code research + Claude rewrite
    files: [src/services/prompt-rewriter.ts, src/services/openai-service.ts]
  - task: Fixed critical CLI stdin bug (--disallowed-tools consuming positional prompt arg)
    files: [src/services/claude-cli-executor.ts]
  - task: Added DB columns for original/rewritten prompt storage
    files: [src/database/migrations/014_add_prompt_rewriting_columns.ts, src/database/migrations/index.ts, src/database/types.ts]
  - task: Added updateTaskRewriteData() to job-manager
    files: [src/services/job-manager.ts]
  - task: Fixed lastMessage bug (was only last line of output, now uses full result.log)
    files: [src/services/prompt-rewriter.ts]

blockers:
  - Research phase is too heavy for MVP — Claude Code researching the target repo adds complexity and latency
  - The full 3-step pipeline is doing more than needed

questions:
  - What prompt rewriting best practices (from reputable sources like Anthropic docs, OpenAI cookbook) should guide the rewrite?
  - Should the rewrite be a single OpenAI call with a well-crafted system prompt?

decisions:
  - MVP scope: Drop the research phase entirely. Single OpenAI call to rewrite the ticket using coding-agent best practices.
  - Keep the DB columns (original_description, rewritten_description) — still useful to store both versions
  - Keep the --rewrite-prompt CLI flag — just simplify what it does
  - Reference branch: feature/prompt-rewriting-research — use as inspiration but strip it down

findings:
  - Research on prompt rewriting for reasoning models: prompt engineering has diminishing returns on o1/o3; the value is in data quality (removing noise, grounding in facts) not CoT tricks
  - The Wharton study confirms CoT provides negligible gains on reasoning models
  - The right framing: "data quality problem, not a prompting technique problem"
  - OpenAI extractResearchQuestions() already works well — filter bug fixed (endsWith('?') → length > 10)
  - Claude CLI stdin fix was critical and affected ALL CLI execution, not just rewriting

worked:
  - Passing prompt via stdin to Claude CLI (instead of positional arg) to avoid --disallowed-tools parsing bug
  - Using result.log instead of result.lastMessage to capture full Claude output

failed:
  - 3-step pipeline is over-engineered for MVP — research phase added complexity without proportional value for the use case

next:
  - Checkout branch feature/prompt-rewriting-research
  - Strip out runClaudeResearch() and the 3-step pipeline from prompt-rewriter.ts
  - Replace with single OpenAI call: take the raw ticket, apply best-practice rewriting prompt for coding agents
  - Research best practices for coding agent prompts (Anthropic docs, OpenAI cookbook, Claude Code system prompt patterns)
  - Format the rewrite to: clear task statement, relevant context only, acceptance criteria as checkboxes, remove Slack noise/assignee info/speculative context
  - Keep DB storage of original + rewritten
  - Keep --rewrite-prompt flag behavior

files:
  created:
    - src/services/prompt-rewriter.ts
    - src/database/migrations/014_add_prompt_rewriting_columns.ts
    - thoughts/shared/specs/2026-03-05-prompt-rewriting.md
    - thoughts/shared/plans/2026-03-05-prompt-rewriting-implementation.md
  modified:
    - src/index.ts
    - src/services/openai-service.ts
    - src/services/job-manager.ts
    - src/services/task-executor.ts
    - src/services/claude-cli-executor.ts
    - src/database/migrations/index.ts
    - src/database/types.ts
    - src/types/non-interactive-config.ts
